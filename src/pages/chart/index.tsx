import React from 'react';
import { Card } from 'antd';
import { FlexCol } from '@/components/tailwind';
const Page = () => {
  return (
    <FlexCol style={{ padding: 20 }}>
      <Card title="Echars 坐标轴类" style={{ width: '100%' }}>
        123
      </Card>
    </FlexCol>
  );
};
export default Page;
